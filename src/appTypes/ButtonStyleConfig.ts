export default interface ButtonStyleConfig {
  backgroundColor: string;
  textColor: string;
  hoverColor: string;
  borderRadius: string;
  padding: string; // Example, '6px 16px'
  fontSize: string; // Example, '0.875rem'
  boxShadow: string;
  // Can add hover styles, for example: hoverBoxShadow
}